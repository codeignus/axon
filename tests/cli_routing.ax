import
    compiler

test check_returns_ok_for_valid_project
    result := compiler.check("")
    assert_eq(result, "ok")

test build_calls_check_then_emits
    result := compiler.build()
    assert_eq(result, result)

test run_calls_build_then_launches
    result := compiler.run()
    assert_eq(result, result)
