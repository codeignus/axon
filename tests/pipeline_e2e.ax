import
    compiler/proj

test pipeline_e2e_check_full_path
    assert_eq(proj.describe_project(), "project:axon bin:axon main:./src/main.ax")
    assert_eq(proj.discover_entry(), "./src/main.ax")
    assert_eq(proj.resolve_check_target(), "check:project")
