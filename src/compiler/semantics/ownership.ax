// Ownership semantics wrappers route through bridge-backed checks until
// the Axon-native ownership engine fully replaces sidecar logic.
pub func analyze_ownership(source: String) String
    return run_ownership_check(source)

pub func check_last_use(source: String) String
    return run_ownership_check(source)
