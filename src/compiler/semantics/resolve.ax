// Resolver-facing API currently delegates to shared semantic snippet checks.
// Keeping this wrapper allows resolver-specific diagnostics to be added later.
pub func check_duplicate_names(source: String) String
    return run_semantic_check(source)

pub func check_imports(source: String) String
    return run_semantic_check(source)
