test compiler_error_shapes_failure
    assert_eq(compiler_error("syntax", "bad token"), "error: stage=syntax code=E1000 reason=bad token")

test internal_compiler_error_shape
    assert_eq(internal_compiler_error("unexpected"), "error: stage=internal code=E0001 reason=unexpected")
