project axon
    version: "0.1.0"

bin axon
    main: "./src/main.ax"

rust_deps
    clap = { version = "4.5", features = ["std"] }
    tracing = "0.1"
    tracing-subscriber = { version = "0.3", features = ["fmt"] }
